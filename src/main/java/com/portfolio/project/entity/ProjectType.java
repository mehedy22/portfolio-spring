package com.portfolio.project.entity;

/** Mirrors {@code ck_project_type}. Optional — a project need not declare a type. */
public enum ProjectType {
	PERSONAL,
	PROFESSIONAL,
	OPEN_SOURCE,
	CLIENT
}
